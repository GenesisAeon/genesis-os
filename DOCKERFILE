# Simple multi-stage build for a Node app served via Nginx
FROM node:18 AS build
WORKDIR /app
COPY . .
RUN npm install && npm run build || true

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
